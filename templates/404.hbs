<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="./css/styles.css">
        <link rel="icon" href="/img/weather.png">
        <title>Weather</title>
    </head>
    <body>
        {{>header}}
        {{ message }}
        <div>
            {{>footer}}
        </div>
    </body>
</html>